{{ if .Values.envoy.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sellia.fullname" . }}-envoy-config
  labels:
    {{- include "sellia.labels" . | nindent 4 }}
data:
  envoy.yaml: |
{{- include "sellia.envoyConfig" . | nindent 4 }}
{{ end }}
