"""External integrations: Slack, Google Calendar, Docusign, Stripe, Zapier.

Also holds the registry of which external APIs each SellIA tool needs and which
of them are really configured on this deployment (registry.py / api.py).
"""
